# Leave unset for local dev (Vite proxies /api to the local backend).
# In production (Vercel), set this to your deployed Render backend URL,
# e.g. https://saifi-saidi-backend.onrender.com
VITE_API_BASE_URL=
